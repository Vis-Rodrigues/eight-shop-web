import React from "react";
import logo from '../../Assets/logo.png';

const NotFound = () => (
  <div className="not-found">
    <img src={logo} className="logo" alt="logo" />
    <h2>Desculpa, não encontramos o produto :(</h2>

  </div>
);

export default NotFound;
